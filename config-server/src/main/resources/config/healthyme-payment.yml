spring:
  application:
    name: healthyme-payment
  jackson:
    serialization:
      write-dates-as-timestamps: false
    deserialization:
      fail-on-unknown-properties: false
    date-format: yyyy-MM-dd HH:mm:ss
    default-property-inclusion: non_null
    property-naming-strategy: SNAKE_CASE
    time-zone: America/Lima
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:healthyme_payment}?createDatabaseIfNotExist=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:j0s33nr1qu3}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: ${PORT:8085}
  error:
    include-message: always
    include-binding-errors: always

stripe:
  key:
    secret: ${STRIPE_SECRET_KEY:sk_test_51RNiRqEPwXgcSj17AFBXg4SH7No83FCGjLvAIJHmgww9ip2INMKeOJOk80mWMuBascyi5uPxw2OR5AJYSRpUnh4q00d3D75rxD}
  webhook:
    secret: ${STRIPE_WEBHOOK_SECRET:whsec_1234567890abcdef}
